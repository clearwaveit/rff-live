# SMTP Configuration for Contact Form
# Update these values with your actual SMTP provider details (e.g., SendGrid, Mailgun, AWS SES, or your hosting provider)

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_username
SMTP_PASSWORD=your_password
SMTP_SECURE=false # Set to true if using port 465
SMTP_FROM_EMAIL=no-reply@recycleforfuture.com
